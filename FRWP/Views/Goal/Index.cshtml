@model IEnumerable<FRWP.Models.Goal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GamePlayer.GamePlayerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOwnGoal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeScored)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GamePlayer.GamePlayerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsOwnGoal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeScored)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
